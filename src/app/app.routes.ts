
import { Routes } from '@angular/router';
import { RequisitionFormComponent } from './requisition-form/requisition-form.component';

export const routes: Routes = [
  { path: '', component: RequisitionFormComponent }, 
];
